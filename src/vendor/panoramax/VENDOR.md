# Vendored: maplibre-gl-panoramax 0.2.0 — VERBATIM, do not edit

`gesture.js` (pose-editor algebra) and `edit.js` (its dependency: pose
clamping/PATCH builders) are byte-for-byte the released npm package
`maplibre-gl-panoramax@0.2.0` (`package/src/*` from the registry tarball,
verified identical 2026-08-12 — #118).

To update: copy `src/gesture.js` + `src/edit.js` from the next release
unchanged and run the full suite. Local changes go upstream first, never
here.
